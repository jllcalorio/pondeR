% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dend.R
\name{plot_dend}
\alias{plot_dend}
\title{Plot a Hierarchical Clustering Dendrogram}
\usage{
plot_dend(
  x,
  clustering_distance_rows = "euclidean",
  clustering_distance_cols = "euclidean",
  agglomeration_method = "ward.D",
  orientation = "rows",
  theme = "nature",
  title = NULL,
  subtitle = NULL,
  global_font_size = 15,
  axis_title_size = NULL,
  axis_text_size = NULL,
  legend_title_size = NULL,
  legend_text_size = NULL,
  plot_title_size = NULL,
  plot_subtitle_size = NULL,
  label_size = NULL,
  line_width = 0.5,
  line_color = "black",
  ...
)
}
\arguments{
\item{x}{A data frame, tibble, or matrix of numeric values to cluster.
Column names may contain special characters.}

\item{clustering_distance_rows}{A single character string specifying the
distance metric for row clustering. One of \code{"euclidean"} (default),
\code{"maximum"}, \code{"manhattan"}, \code{"canberra"},
\code{"binary"}, or \code{"minkowski"}. See \code{\link[stats]{dist}}.}

\item{clustering_distance_cols}{A single character string specifying the
distance metric for column clustering. Same options as
\code{clustering_distance_rows}. Default \code{"euclidean"}.}

\item{agglomeration_method}{A single character string specifying the
agglomeration method passed to \code{\link[stats]{hclust}}. One of
\code{"ward.D"} (default), \code{"ward.D2"}, \code{"single"},
\code{"complete"}, \code{"average"}, \code{"mcquitty"},
\code{"median"}, or \code{"centroid"}.}

\item{orientation}{A single character string. One of \code{"rows"} (default;
clusters rows), \code{"cols"} (clusters columns), or \code{"both"}
(returns a named list of two \code{ggplot2} objects).}

\item{theme}{A single character string specifying the plot theme. One of
\code{"nature"} (default), \code{"classic"}, \code{"minimal"}, or
\code{"bw"}.}

\item{title}{A single character string for the plot title. Default
\code{NULL} (no title).}

\item{subtitle}{A single character string for the plot subtitle. Default
\code{NULL} (no subtitle).}

\item{global_font_size}{A single positive numeric specifying the base font
size in points. Default \code{15}.}

\item{axis_title_size}{A single positive numeric for axis title font size.
Defaults to \code{global_font_size}.}

\item{axis_text_size}{A single positive numeric for axis text font size.
Defaults to \code{global_font_size}.}

\item{legend_title_size}{A single positive numeric for legend title font
size. Defaults to \code{global_font_size}.}

\item{legend_text_size}{A single positive numeric for legend text font size.
Defaults to \code{global_font_size}.}

\item{plot_title_size}{A single positive numeric for plot title font size.
Defaults to \code{global_font_size + 2}.}

\item{plot_subtitle_size}{A single positive numeric for plot subtitle font
size. Defaults to \code{global_font_size}.}

\item{label_size}{A single positive numeric controlling the font size of
leaf labels. Defaults to \code{global_font_size * 0.8}.}

\item{line_width}{A single positive numeric for dendrogram branch width.
Default \code{0.5}.}

\item{line_color}{A single character string for branch colour.
Default \code{"black"}.}

\item{...}{Additional arguments passed to \code{\link[stats]{dist}} or
\code{\link[stats]{hclust}}.}
}
\value{
A \code{ggplot2} object, or a named list of two \code{ggplot2}
objects (\code{rows} and \code{cols}) when \code{orientation = "both"}.
}
\description{
Produces a publication-ready dendrogram from a data frame, tibble, or
matrix using hierarchical clustering. Supports row-wise and column-wise
clustering with configurable distance metrics and agglomeration methods.
Returns a \pkg{ggplot2} object via \pkg{ggdendro}.
}
\details{
Numeric columns are extracted and scaled (zero mean, unit variance) before
computing distances. Rows or columns containing all \code{NA} values are
silently dropped. Remaining \code{NA}s are imputed with column means before
scaling.

When \code{orientation = "both"}, the returned object is a named list with
elements \code{rows} and \code{cols}.
}
\examples{
## Row dendrogram (default)
plot_dend(mtcars)

## Column dendrogram with Manhattan distance
plot_dend(mtcars, orientation = "cols",
          clustering_distance_cols = "manhattan")

## Both axes
dends <- plot_dend(mtcars, orientation = "both")
dends$rows
dends$cols

## Custom styling
plot_dend(mtcars, theme = "minimal", line_color = "#2166AC",
          line_width = 0.8, global_font_size = 10,
          title = "Sample Clustering")

}
\author{
John Lennon L. Calorio
}
