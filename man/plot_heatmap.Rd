% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Plot a Correlation Heatmap}
\usage{
plot_heatmap(
  x,
  method = "auto",
  out = "all",
  sig_threshold = 0.05,
  remove = NULL,
  theme = "nature",
  title = NULL,
  subtitle = NULL,
  show_rlab = TRUE,
  show_clab = TRUE,
  top_n = NULL,
  bottom_n = NULL,
  color = c("#2166AC", "#F7F7F7", "#B2182B"),
  clab_angle = 45,
  show_triangle = "all",
  show_coef = FALSE,
  round = 2L,
  mark_x = TRUE,
  mark_x_size = 1,
  mark_x_alpha = 0.6,
  show_dend = FALSE,
  dend_row_width = 0.2,
  dend_col_height = 0.2,
  global_font_size = 15,
  axis_title_size = NULL,
  axis_text_size = NULL,
  legend_title_size = NULL,
  legend_text_size = NULL,
  coef_text_size = NULL,
  plot_title_size = NULL,
  plot_subtitle_size = NULL,
  ...
)
}
\arguments{
\item{x}{A data frame, tibble, or matrix. Column names may contain special
characters.}

\item{method}{Correlation method passed to \code{\link{run_correl}}. One of
\code{"auto"} (default), \code{"pearson"}, \code{"spearman"},
\code{"kendall"}, \code{"pointbiserial"}, \code{"phi"}, \code{"cramerv"},
or \code{"poly"}.}

\item{out}{A single character string: \code{"all"} (default; plot all
correlations) or \code{"masked"} (set non-significant cells to
\code{NA}).}

\item{sig_threshold}{Significance threshold passed to
\code{\link{run_correl}}. Default \code{0.05}.}

\item{remove}{Level-exclusion list passed to \code{\link{run_correl}}.}

\item{theme}{A single character string: \code{"nature"} (default),
\code{"classic"}, \code{"minimal"}, or \code{"bw"}.}

\item{title}{A single character string for the plot title. Default
\code{NULL}.}

\item{subtitle}{A single character string for the plot subtitle. Default
\code{NULL}.}

\item{show_rlab}{Logical. Show row labels. Default \code{TRUE}.}

\item{show_clab}{Logical. Show column labels. Default \code{TRUE}.}

\item{top_n}{A single positive integer. Plot only the \code{top_n} pairs
with the highest absolute correlation. Mutually exclusive with
\code{bottom_n}. Default \code{NULL}.}

\item{bottom_n}{A single positive integer. Plot only the \code{bottom_n}
pairs with the lowest absolute correlation. Mutually exclusive with
\code{top_n}. Default \code{NULL}.}

\item{color}{A character vector of length 3 specifying fill colours for
negative, neutral, and positive correlation respectively. Default
\code{c("#2166AC", "#F7F7F7", "#B2182B")}.}

\item{clab_angle}{A single numeric (0–360) controlling the rotation angle
of column (x-axis) labels in degrees. Default \code{45}.}

\item{show_triangle}{A single character string: \code{"all"} (default),
\code{"lower"} (retain lower triangle only), or \code{"upper"} (retain
upper triangle only).}

\item{show_coef}{Logical. Annotate each cell with its rounded correlation
coefficient. Default \code{FALSE}.}

\item{round}{A single non-negative integer. Number of decimal places for
coefficient annotation. Default \code{2}. When rounding produces exactly
\code{1} or \code{-1} for a non-unity value, scientific notation is used
and the font is reduced. Ignored when \code{show_coef = FALSE}.}

\item{mark_x}{Logical. Overlay an \code{"\u00D7"} on non-significant cells
when \code{show_coef = TRUE}. Default \code{TRUE}.}

\item{mark_x_size}{A single positive numeric acting as a multiplier for the
size of the "×" mark. Default \code{1.0} (fills ~85\% of the cell).
Ignored when \code{mark_x = FALSE}.}

\item{mark_x_alpha}{A single numeric (0–1) controlling the transparency of
the "×" mark. Default \code{0.6}. Ignored when \code{mark_x = FALSE}.}

\item{show_dend}{Logical. Whether to attach hierarchical clustering
dendrograms to the heatmap margins. Requires \pkg{patchwork}.
Default \code{FALSE}.}

\item{dend_row_width}{A single positive numeric controlling the relative
width of the row dendrogram panel when \code{show_dend = TRUE}.
Default \code{0.2}.}

\item{dend_col_height}{A single positive numeric controlling the relative
height of the column dendrogram panel when \code{show_dend = TRUE}.
Default \code{0.2}.}

\item{global_font_size}{A single positive numeric specifying the base font
size in points. Default \code{15}.}

\item{axis_title_size}{Font size for axis titles. Defaults to
\code{global_font_size}.}

\item{axis_text_size}{Font size for axis tick labels. Defaults to
\code{global_font_size}.}

\item{legend_title_size}{Font size for the legend title. Defaults to
\code{global_font_size}.}

\item{legend_text_size}{Font size for legend text. Defaults to
\code{global_font_size}.}

\item{coef_text_size}{Font size for in-cell coefficient annotations.
Defaults to \code{global_font_size * 0.7}. Ignored when
\code{show_coef = FALSE}.}

\item{plot_title_size}{Font size for the plot title. Defaults to
\code{global_font_size + 2}.}

\item{plot_subtitle_size}{Font size for the plot subtitle. Defaults to
\code{global_font_size}.}

\item{...}{Additional arguments passed to \code{\link{run_correl}} and,
when \code{show_dend = TRUE}, to \code{\link{plot_dend}}.}
}
\value{
A \code{ggplot2} object, or a \pkg{patchwork} composite when
\code{show_dend = TRUE}.
}
\description{
Produces a publication-ready correlation heatmap with optional hierarchical
clustering dendrograms, significance masking, triangle display, and
in-cell coefficient annotations. Internally calls \code{\link{run_correl}}
and returns a \pkg{ggplot2} object.
}
\details{
Hierarchical clustering uses Ward's D2 linkage on \code{1 - |r|} as the
dissimilarity to reorder variables before plotting.

When \code{show_dend = TRUE}, dendrograms are composed with the heatmap
using \pkg{patchwork}. The row dendrogram is placed to the left and the
column dendrogram above the heatmap. Arguments accepted by
\code{\link{plot_dend}} (e.g. \code{agglomeration_method},
\code{line_color}) are forwarded automatically via \code{...}.
}
\examples{
## Basic heatmap
plot_heatmap(mtcars)

## Masked, lower triangle only
plot_heatmap(mtcars, out = "masked", show_triangle = "lower")

## Show coefficients
plot_heatmap(mtcars, show_coef = TRUE, mark_x = TRUE)

## With dendrograms
plot_heatmap(mtcars, show_dend = TRUE)

}
\author{
John Lennon L. Calorio
}
