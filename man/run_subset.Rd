% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_subset.R
\name{run_subset}
\alias{run_subset}
\title{Balanced Subsetting of Rows or Columns}
\usage{
run_subset(
  x,
  n = NULL,
  match = NULL,
  independent = TRUE,
  balance = "within_match",
  group = NULL,
  by = "row",
  random = TRUE,
  top_to_bottom = TRUE,
  left_to_right = TRUE,
  seed = 123
)
}
\arguments{
\item{x}{A data frame, tibble, or matrix to be subsetted.}

\item{n}{An integer specifying the desired number of rows (when
\code{by = "row"}) or columns (when \code{by = "col"}) in \verb{$chosen}.}

\item{match}{\code{NULL} (default) or a character vector of one or more
column names in \code{x} containing categorical variables to balance on.}

\item{independent}{Logical; \code{TRUE} (default). When \code{match} has multiple columns:
If \code{TRUE}, each column is balanced independently and their results are unioned.
If \code{FALSE}, columns are crossed into a single interaction and balanced jointly.}

\item{balance}{A string; either \code{"within_match"} (default) or \code{"within_group"}.
Controls the direction of balancing when both \code{match} and \code{group} are provided.
\itemize{
\item \strong{\code{"within_match"}}: For every category in \code{match}, the levels of \code{group}
will have an equal number of observations. For example, if balancing by Age Group,
each Age Group will have an equal number of Diseased and Healthy patients.
\item \strong{\code{"within_group"}}: For every category in \code{group}, the levels of \code{match}
will have an equal number of observations. For example, within the Diseased
group, there will be an equal number of patients from each Age Group.
}}

\item{group}{\code{NULL} (default) or a character vector of column names
that define additional grouping strata.}

\item{by}{A string; either \code{"row"} (default) or \code{"col"}.}

\item{random}{Logical; \code{TRUE} (default) to select rows or columns randomly.}

\item{top_to_bottom}{Logical; \code{TRUE} (default). For deterministic row pulls.}

\item{left_to_right}{Logical; \code{TRUE} (default). For deterministic col pulls.}

\item{seed}{An integer; \code{123} (default). The random seed. \code{NULL} disables seeding.}
}
\value{
A named list of class \code{"run_subset"}.
}
\description{
Subsets a data frame, tibble, or matrix either row-wise or column-wise,
with optional balancing across categorical groupings. When balancing is
requested via \code{match}, the function ensures that every category within
each specified column is represented by the same number of observations.
An optional \code{n} argument acts as a row ceiling: the per-category count
is derived so that the chosen set is as close to \code{n} as possible while
remaining perfectly balanced within each \code{match} column.

When \code{match} contains more than one column, the \code{independent} argument
controls how they are combined:
\itemize{
\item \strong{\code{independent = TRUE} (default):} Each \code{match} column is
balanced separately. A row is selected if it satisfies the balance
requirement of \strong{any} \code{match} column (union). This maximises the
number of chosen rows.
\item \strong{\code{independent = FALSE}:} All \code{match} columns are crossed
into a single Cartesian product of categories (e.g.
\code{"1-2 years × Male"}), and every unique combination is represented
equally. This guarantees perfect joint balance.
}
}
\author{
John Lennon L. Calorio
}
