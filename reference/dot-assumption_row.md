# Build the standard assumptions data frame row from a normality result

Build the standard assumptions data frame row from a normality result

## Usage

``` r
.assumption_row(label, method_name, p_value, violated)
```
