# Execute an independent two-sample test

Execute an independent two-sample test

## Usage

``` r
.run_independent_two_sample(
  y,
  groups,
  grp,
  normality_violated,
  variance_violated,
  test_type,
  warnings_list
)
```
